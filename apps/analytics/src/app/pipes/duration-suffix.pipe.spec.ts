import { DurationSuffixPipe } from './duration-suffix.pipe';

describe('DurationSuffixPipe', () => {
  it('create an instance', () => {
    const pipe = new DurationSuffixPipe();
    expect(pipe).toBeTruthy();
  });
});
